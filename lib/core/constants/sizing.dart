import 'package:flutter/widgets.dart';

const double kMaxPadding = 24; // bigger default padding
const double kDefaultPadding = 16; // standard padding using default items
const double kItemPadding = 10; // default padding between child widgets
const double kMinPadding = 5; // minimum default padding

const double kDefaultIconSize = 18;

const BorderRadius kDefaultBorderRadius = BorderRadius.all(Radius.circular(8));
